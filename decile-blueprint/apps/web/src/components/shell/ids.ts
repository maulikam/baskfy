/** Shared element ids, so the skip link and its target cannot drift apart. */
export const MAIN_CONTENT_ID = "main-content";
/**
 * The primary navigation row. Was `app-sidebar` until M36 moved navigation to the top; the id
 * changed with it rather than keeping a name that describes a layout the app no longer has.
 */
export const PRIMARY_NAV_ID = "primary-nav";
