[project]
name = "baskfy-execution"
version = "0.1.0"
description = "The only path to an order: guards -> risk -> rate limits -> journal -> broker."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/baskfy_execution"]
