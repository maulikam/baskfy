{
  "factors.py:114:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `FactorConfig`. Equivalent: `slots` changes the instance layout, not any value the engine computes. The `frozen=True` on the same line IS a real invariant — `DEFAULT_FACTOR_CONFIG` is a module-level singleton used as a default argument, so a mutable one would let a caller recalibrating the circuit bands silently recalibrate the nightly pipeline in the same worker — and it is killed by `test_factor_edge_guards.TestTheConfigObjectsAreImmutable`. Killing the `slots` half would need a test asserting `__dict__` is absent, which asserts CPython's object model rather than docs/05.",
  "factors.py:128:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `FactorResult`. Same as above and weaker still: a two-field container returned once per call.",
  "factors.py:318:19:comparison": "`abs(vol_N) < ZERO_VOLATILITY_EPSILON` -> `<=`. The two readings differ only for a volatility exactly equal to 5e-11, one representable float out of 2^64, which `rolling_std` cannot be made to produce by any series a test could construct. docs/05 §3's rule is \"if vol_N == 0 -> NULL\"; the epsilon exists because a constant series has a floating-point stdev around 1e-17 (see the constant's own comment), and which side of the boundary the exact value falls on is arbitrary under either reading.",
  "factors.py:405:23:numeric_constant": "`if frame.height == 0` -> `== 1` in `_with_rsi`. Equivalent, because of the guard eight lines below it. With the mutant an empty frame falls through to `frame.pivot(...)`, which yields no dates, and `if len(dates) < 2` then returns the same all-NULL columns by the other route. `test_factor_edge_guards.TestFramesTooShortToComputeAnything` asserts both the empty and the single-bar panel produce NULL RSI and no exception; it cannot distinguish which of the two guards produced them, and nothing downstream can either.",
  "factors.py:418:21:comparison": "`gains = np.where(changes > 0, changes, 0.0)` -> `>= 0`. Equivalent by arithmetic: where `changes == 0` the true branch selects `changes`, which *is* 0.0, and the false branch selects the literal 0.0. docs/05 §5's `gain_t = max(P_t - P_{t-1}, 0)` is satisfied identically by both.",
  "factors.py:419:22:comparison": "`losses = np.where(changes < 0, -changes, 0.0)` -> `<= 0`. Equivalent by arithmetic: where `changes == 0` the true branch selects `-0.0` and the false branch the literal `0.0`, and `-0.0 == 0.0` is True in IEEE 754, so `avg_loss == 0` — docs/05 §5's \"RSI = 100 when avg_loss == 0\" rule — fires identically under both. The sibling mutant on the line above (`changes > 0` -> `>= 0`) is the same argument for gains.",
  "swing/indicators.py:54:33:comparison": "`dtype = pl.String if column == \"symbol\" else pl.Float64` -> `!=` in `_prepare`, the placeholder dtype for an *absent* optional column. Equivalent on every output: the numeric placeholders are `.cast(pl.Float64)` on the next line and `symbol` is `.cast(pl.String)` by `setups._conform`, and a null column of either dtype casts to a null column of the other. `test_swing_contract_edges.TestTheIndicatorDefaultsAndGuards` asserts the values (null, 1.0) that come out; the dtype in between is not a value.",
  "swing/setups.py:126:83:boolean_constant": "`group_by(instrument_id, maintain_order=True)` -> `False` in `_window`. The flag only fixes the order of the per-instrument groups inside one detector's frame; `detect_setups` sorts every row by `(setup, -score, instrument_id)` before the worker or the page reads it (asserted by `test_detect_setups_ranks_best_score_first_within_a_setup_and_ties_by_instrument`), so the group order is unobservable through the contract. A test pinning one detector's row order would assert an implementation detail, not `04`.",
  "swing/setups.py:157:48:boolean_constant": "`group_by(instrument_id, maintain_order=True)` -> `False` in `detect_flags`. The flag only fixes the order of the per-instrument groups inside one detector's frame; `detect_setups` sorts every row by `(setup, -score, instrument_id)` before the worker or the page reads it (asserted by `test_detect_setups_ranks_best_score_first_within_a_setup_and_ties_by_instrument`), so the group order is unobservable through the contract. A test pinning one detector's row order would assert an implementation detail, not `04`.",
  "swing/setups.py:174:36:numeric_constant": "`pole_high = high.slice(0, n - 1).max()` -> `slice(0, n - 2)`: the pole high is searched without yesterday. Equivalent for every row the detector can emit: if yesterday's high were the highest before today, `pole_idx` (its own site, killed) would be `n - 2`, `base_bars` would be 1, and both the SETTING_UP path (`base_bars >= base_min_bars` [10]) and the breakout path (`_base_ok`) refuse the row — so the only bars where the two slices disagree are bars that produce no row.",
  "swing/setups.py:278:48:boolean_constant": "`group_by(instrument_id, maintain_order=True)` -> `False` in `detect_eps`. The flag only fixes the order of the per-instrument groups inside one detector's frame; `detect_setups` sorts every row by `(setup, -score, instrument_id)` before the worker or the page reads it (asserted by `test_detect_setups_ranks_best_score_first_within_a_setup_and_ties_by_instrument`), so the group order is unobservable through the contract. A test pinning one detector's row order would assert an implementation detail, not `04`.",
  "swing/setups.py:345:48:boolean_constant": "`group_by(instrument_id, maintain_order=True)` -> `False` in `detect_parabolic`. The flag only fixes the order of the per-instrument groups inside one detector's frame; `detect_setups` sorts every row by `(setup, -score, instrument_id)` before the worker or the page reads it (asserted by `test_detect_setups_ranks_best_score_first_within_a_setup_and_ties_by_instrument`), so the group order is unobservable through the contract. A test pinning one detector's row order would assert an implementation detail, not `04`.",
  "swing/setups.py:341:60:numeric_constant": "`_window(indicated, as_of, min_up_streak + 1)` -> `+ 2` in `detect_parabolic`: one more bar per instrument. Equivalent: every aggregation in the parabolic detector is `.last()` or `.shift(1).last()`, which read the same two bars from any window of two or more; the streak itself comes from `indicators.up_streak`, computed over the whole history. The `- 1` mutant on the same line is killed (`test_a_single_green_day_is_a_run_when_the_config_says_so`: a window of zero bars detects nothing).",
  "swing/setups.py:370:15:comparison": "`extended = dist_ma_fast_pct >= min_extension_adr * adr_pct` -> `>`. `04` §4.2 says `>=`, and the inclusive reading is asserted a hair either side of the measured multiple by `test_the_extension_is_measured_in_adrs_above_the_ten_day`. The boundary itself is an equality between two *measurements* (a percent distance and a multiple times a percent), so a test at the exact value would assert that `(d / a) x a == d` round-trips in IEEE-754 for the fixture's particular floats — a statement about floating point, not about the rule.",
  "swing/setups.py:412:57:boolean_constant": "`sort([setup, score, instrument_id], descending=[False, True, False])` -> the *setup* key descending. `detect_setups` promises \"best score first within each setup\"; the order of the setup groups themselves (EP, FLAG, PARABOLIC_SHORT) is not a rule anywhere in `04`, and the rows of one setup stay contiguous either way (asserted by `test_detect_setups_ranks_best_score_first_within_a_setup_and_ties_by_instrument`, which kills the other two flips on this line).",
  "swing/sizing.py:63:7:comparison": "`_pct`: `if denominator <= 0: return 0` -> `< 0`. Unreachable at zero: every caller guards the denominator first — `_refuse` passes `entry` only `if entry > 0`, `size_position` has returned `NO_EQUITY` before `_pct(value, equity)`, and `implied_risk_pct` returns 0 for `equity <= 0` on its own line. The two guards overlap by design (`04` §5.1's refusal order is the outer one).",
  "swing/sizing.py:76:55:comparison": "`_refuse`: `stop_distance_pct = _pct(entry - stop, entry) if entry > 0 else 0` -> `>= 0`. At `entry == 0` the mutant calls `_pct(-stop, 0)`, whose own `denominator <= 0` guard returns 0 — the same 0. A double guard; either half alone would do, and the refusal (`STOP_NOT_BELOW_ENTRY` for `entry <= 0`) is asserted by `TestTheRefusalsAndTheirBoundaries`.",
  "swing/sizing.py:149:7:comparison": "`implied_risk_pct`: `if equity <= 0: return 0` -> `< 0`. At `equity == 0` the mutant falls through to `_pct(risk, 0)`, whose own guard returns 0. Same double guard as `sizing.py:76`.",
  "swing/stops.py:130:47:numeric_constant": "`partial_quantity`: `min(max(part, 0), max(quantity - 1, 0))` -> the second `0` becomes `1`. Equivalent: `max(quantity - 1, 1)` differs from `max(quantity - 1, 0)` only for `quantity <= 1`, where `part = quantity // 3` is 0 and the outer `min` returns 0 either way. `TestThePartialQuantity` and `TestThePartialsSmallestCases` pin 0, 1, 2, 3 and 4 shares.",
  "swing/market.py:140:24:comparison": "`drawdown_pct`: `if peak <= 0 or equity >= peak: return 0.0` -> `> peak`. At `equity == peak` the mutant computes `(peak - equity) / peak x 100 = 0.00`, quantized to the same `0.0`. Equivalent by arithmetic; `04` §8.5's \"0 at or above the peak\" holds under both.",
  "swing/market.py:165:26:numeric_constant": "`exposure_tier(..., drawdown_pct: float = 0.0)` -> default `1`. The default is only ever read against `max_drawdown_pct` [15] and `resume_drawdown_pct` [10]; a call that names no drawdown gets the same rung for 0 and for 1 under any config whose resume line is above 1% — and the worker always passes the measured value (SW9.5.1). A test would have to assert the literal default, which is not a rule.",
  "swing/market.py:166:32:boolean_constant": "`exposure_tier(..., was_drawdown_locked: bool = False)` -> default `True`. With the default `drawdown_pct` of 0 the hysteresis `was_locked and 0 > resume` is False either way, so a call that names neither argument is not locked under both. Same reasoning as `market.py:165`; the worker passes the stored state.",
  "swing/opening_range.py:169:49:comparison": "`live_gap`: `ratio = volume / expected if expected > 0 else 0` -> `>= 0`. Unreachable at zero: `expected = avg_daily_volume x minutes / 375` and both factors have been refused as `<= 0` four lines earlier (`TestTheLiveGapGuards`), so `expected` is strictly positive whenever this line runs.",
  "swing/backtest.py:147:26:numeric_constant": "`_MIN_WINDOW_BARS = 2` -> `3`: the shortest per-instrument slice the detection window keeps. A two-bar name cannot set up (a flag needs a 10-bar base, an EP 60 prior bars, a runner a 3-bar streak), so dropping it a bar earlier changes no detection; SW9 justified the same.",
  "swing/backtest.py:330:75:numeric_constant": "`BacktestResult.drawdown: DrawdownSummary = DrawdownSummary(0, 0, 0, None, 0)` -> the default's `lock_sessions` becomes 1. `run_backtest` always builds the result with `primary.drawdown_summary()`; the field default is read by nothing in the engine (an empty range returns through the same constructor with the summary of an empty book).",
  "swing/backtest.py:432:7:comparison": "`_drawdown`: `if equity >= peak: return 0` -> `>`. At `equity == peak` the mutant computes `(peak - equity) / sleeve = 0` — the same 0 (`market.py:140` is the live twin).",
  "swing/backtest.py:472:11:comparison": "`_Panel.row_of`: the `index >= len(instruments)` half of the guard. Unreachable: a candidate detected today has a bar today, so the instrument is in the panel and `searchsorted` lands on it; the guard exists for a caller that asks about a name the panel never held (SW9).",
  "swing/backtest.py:472:11:boolean_operator": "`_Panel.row_of`: `index >= len or instruments[index] != id` -> `and`. Same unreachable guard as the comparison on this line: every instrument the engine asks for is in the panel.",
  "swing/backtest.py:530:40:numeric_constant": "`_IndexSeries.reading_on`: `close=float(closes[n - 1])` -> `n - 2`: the reading's `close` is yesterday's. `market_gate` reads only `long_bias` / `bearish` (the 10/20-day MAs — `04` §8.3: \"the close itself is not consulted\"), and the backtest stores no index close, so the mutant changes no session's gate and no reported number. `IndexReading.close` exists for the page's market card, which the worker fills from `index_snapshot_daily` directly.",
  "swing/backtest.py:555:19:numeric_constant": "`_Position.missing: int = 0` -> `1`. The entry day's bar sets `missing = 0` before the counter is ever compared (`_missing` only runs on a session with no bar, after entry), so the default is dead on every path; SW9 justified the same.",
  "swing/backtest.py:656:31:boolean_constant": "`self.drawdown_locked = False` -> `True` at construction. The lock state is recomputed from the sleeve's drawdown at every close (`drawdown_locked(0.0, was_locked=True)` is False: nothing is below its peak on day one) before the next open's entries are decided, and no entry can happen on a run's first session (a detection needs a close; the entry is the next open). Equivalent for every run.",
  "swing/sizing.py:42:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `SizedPosition`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/stops.py:57:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `OpenPosition`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/stops.py:73:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `DailyBar`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/stops.py:87:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `Action`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/market.py:40:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `BreadthSnapshot`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/market.py:50:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `IndexReading`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/market.py:68:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `ExposureTier`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/opening_range.py:34:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `Candle`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/opening_range.py:46:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `OpeningRange`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/opening_range.py:65:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `TriggerVerdict`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/opening_range.py:147:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `LiveGapVerdict`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/plan.py:73:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `WatchItem`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/plan.py:92:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `SwingAccount`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/plan.py:102:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `PlanLine`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/plan.py:116:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `Skipped`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/plan.py:123:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `SwingPlan`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/journal.py:22:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `ClosedTrade`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/journal.py:50:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `JournalStats`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:200:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `BacktestParams`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:217:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `BacktestTrade`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:248:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `DrawdownSummary`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:263:18:boolean_constant": "`frozen=True` -> `False` on the backtest's private `GateCell`, which the engine never hands out: it is built, read and discarded inside `run_backtest`, so nothing outside the module could observe a mutation. Killing it would need a test that reaches into the module for a private class (SW9 said the same).",
  "swing/backtest.py:263:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `GateCell`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:277:18:boolean_constant": "`frozen=True` -> `False` on the backtest's private `GateContribution`, which the engine never hands out: it is built, read and discarded inside `run_backtest`, so nothing outside the module could observe a mutation. Killing it would need a test that reaches into the module for a private class (SW9 said the same).",
  "swing/backtest.py:277:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `GateContribution`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:286:18:boolean_constant": "`frozen=True` -> `False` on the backtest's private `GateComparison`, which the engine never hands out: it is built, read and discarded inside `run_backtest`, so nothing outside the module could observe a mutation. Killing it would need a test that reaches into the module for a private class (SW9 said the same).",
  "swing/backtest.py:286:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `GateComparison`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:310:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `BacktestResult`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:446:18:boolean_constant": "`frozen=True` -> `False` on the backtest's private `_Panel`, which the engine never hands out: it is built, read and discarded inside `run_backtest`, so nothing outside the module could observe a mutation. Killing it would need a test that reaches into the module for a private class (SW9 said the same).",
  "swing/backtest.py:446:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `_Panel`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:492:18:boolean_constant": "`frozen=True` -> `False` on the backtest's private `_IndexSeries`, which the engine never hands out: it is built, read and discarded inside `run_backtest`, so nothing outside the module could observe a mutation. Killing it would need a test that reaches into the module for a private class (SW9 said the same).",
  "swing/backtest.py:492:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `_IndexSeries`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:541:17:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `_Position`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:558:18:boolean_constant": "`frozen=True` -> `False` on the backtest's private `_Candidate`, which the engine never hands out: it is built, read and discarded inside `run_backtest`, so nothing outside the module could observe a mutation. Killing it would need a test that reaches into the module for a private class (SW9 said the same).",
  "swing/backtest.py:558:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `_Candidate`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/backtest.py:571:18:boolean_constant": "`frozen=True` -> `False` on the backtest's private `_Close`, which the engine never hands out: it is built, read and discarded inside `run_backtest`, so nothing outside the module could observe a mutation. Killing it would need a test that reaches into the module for a private class (SW9 said the same).",
  "swing/backtest.py:571:30:boolean_constant": "`@dataclass(slots=True)` -> `slots=False` on `_Close`. Equivalent: `slots` changes the instance layout, not any value the engine computes — the same mutant already justified for `factors.FactorConfig`. The `frozen=True` half of the decorator is a real invariant and is killed by `test_swing_contract_edges.TestNothingTheEngineHandsOutCanBeMutated`.",
  "swing/plan.py:142:42:boolean_constant": "`SwingPlan.plan_hash`: `json.dumps(payload, sort_keys=True, ...)` -> `sort_keys=False`. `04` §9 says the desk's `plan_id` derives from the hash of the canonical lines; with the flag off the keys come out in `asdict`'s field order, which the dataclass fixes, so the id is still deterministic and still tells two plans apart (`TestThePlanArithmetic` / the safety properties assert both). The flag makes the canonical form independent of field order — a robustness the document does not state; a test pinning the exact digest would assert a serialisation, not a rule.",
  "swing/backtest.py:657:95:numeric_constant": "`_Book.__init__`: the starting `DrawdownSummary(..., locked_sessions=0)` -> `1`. Equivalent: `drawdown_summary()` (the only reader the result sees) rebuilds the summary with the book's own `self.locked_sessions` counter, and every deeper drawdown replaces the whole record at line 967 with the counter too, so the constructor's literal is never reported.",
  "swing/backtest.py:755:27:arithmetic": "`cash_available = sleeve_inr - open_cost` -> `+ open_cost` in the backtest's account. Not equivalent in principle, and not reached by the fixture set: at every rung below the top the tier's exposure ceiling (25/50/75% of the sleeve) is tighter than the cash left, so a position that would overrun cash is refused `EXPOSURE_FULL` under both readings; at the top rung the two bounds coincide (`exposure + value <= sleeve` is `value <= cash`), and the readings differ only for a book already near 100% invested — the true one sizes the last name down to the cash (`CASH` cap), the mutant lets it be refused. Reaching that needs a run that has climbed the ladder to rung 3 with eight-plus concurrent positions, which no planted fixture does (SW9 recorded the same survivor). Killing it is a ten-position backtest fixture — noted in STATUS as SW12's open item, not claimed.",
  "swing/backtest.py:830:11:boolean_operator": "`if self.gate is MarketGate.RED or not self.tier.new_entries_allowed` -> `and`. Equivalent: `exposure_tier` answers RED with `new_entries_allowed=False` (`TestTheLadder`), so RED implies the second operand and the conjunction is the disjunction on every tier the engine can hold; the `or` is the belt to `market.exposure_tier`'s braces (SW9 recorded the same).",
  "swing/backtest.py:846:11:comparison": "`if open_ >= candidate.trigger: fill at the open` -> `>`. Equivalent by construction: an open exactly on the trigger falls through to the next line, `high >= trigger`, which is true whenever `open == trigger`, and fills at the trigger — the same price. `test_swing_backtest` asserts the entry-at-open and entry-at-trigger rules; the boundary between them prices identically (SW9 recorded the same).",
  "swing/backtest.py:1056:38:numeric_constant": "`_close`: the empty-day `BreadthSnapshot(0, 0.0, 0.0, 0.0)` — one of its four literals shifted to 1. A session with no bar for any name reads RED either way: `market_gate` answers RED for a count of 0 and for a one-name universe with 1% up (`red_max_pct_up` is 2), and the backtest stores no breadth row. Equivalent for the gate, which is the only reader (SW9 recorded the same).",
  "swing/backtest.py:1056:41:numeric_constant": "`_close`: the empty-day `BreadthSnapshot(0, 0.0, 0.0, 0.0)` — one of its four literals shifted to 1. A session with no bar for any name reads RED either way: `market_gate` answers RED for a count of 0 and for a one-name universe with 1% up (`red_max_pct_up` is 2), and the backtest stores no breadth row. Equivalent for the gate, which is the only reader (SW9 recorded the same).",
  "swing/backtest.py:1056:46:numeric_constant": "`_close`: the empty-day `BreadthSnapshot(0, 0.0, 0.0, 0.0)` — one of its four literals shifted to 1. A session with no bar for any name reads RED either way: `market_gate` answers RED for a count of 0 and for a one-name universe with 1% up (`red_max_pct_up` is 2), and the backtest stores no breadth row. Equivalent for the gate, which is the only reader (SW9 recorded the same).",
  "swing/backtest.py:1056:51:numeric_constant": "`_close`: the empty-day `BreadthSnapshot(0, 0.0, 0.0, 0.0)` — one of its four literals shifted to 1. A session with no bar for any name reads RED either way: `market_gate` answers RED for a count of 0 and for a one-name universe with 1% up (`red_max_pct_up` is 2), and the backtest stores no breadth row. Equivalent for the gate, which is the only reader (SW9 recorded the same).",
  "swing/backtest.py:1066:54:numeric_constant": "`first = max(col - bars_required + 1, 0)` -> `+ 2`: the detection window one session shorter than `bars_required`. The planted base is 35 bars inside a 126-session window and every detector's own lookback is shorter than the window, so a session less at the far end changes no detection; the look-ahead side is guarded by the detector's `date <= as_of` filter, not by this slice (SW9 recorded the same). The `- 1` twin on this line (a window that starts after today) is killed.",
  "swing/backtest.py:1066:20:arithmetic": "`first = max(col - bars_required + 1, 0)` -> `- 1`: the window one session longer at the far end. Same reasoning as the `+ 2` mutant on this line: nothing the detectors read reaches that far back, and the near end is guarded by `date <= as_of`.",
  "swing/backtest.py:1066:57:numeric_constant": "`max(col - bars_required + 1, 0)` -> `max(..., 1)`: in the run's first `bars_required` sessions the window starts at session 1 instead of 0. The planted fixtures carry their 140-bar history *before* the run's first session, so no detection reads the run's own session 0 as part of a pole; a real run is handed the lookback the same way (`load_swing_bars`, SW9). Equivalent on every fixture; a spec test needs a base that begins on the run's first session.",
  "swing/backtest.py:1070:40:arithmetic": "`window = by_date.slice(lo, hi - lo)` -> `hi + lo`: the slice runs past today. The detector's own `date <= as_of` filter (`setups._window`) is the look-ahead guard, so the extra rows are dropped before any rule reads them — and that is the right place for the guard to live (SW9 recorded the same).",
  "swing/backtest.py:1075:31:comparison": "`pl.len().over(instrument_id) >= _MIN_WINDOW_BARS` -> `>`: a two-bar name is dropped from the window as well as a one-bar name. A two-bar name cannot set up (same argument as `_MIN_WINDOW_BARS = 2 -> 3` at line 147).",
  "swing/backtest.py:1155:11:boolean_operator": "`_delta`: `None if a.max_drawdown_pct is None or b... is None else a - b` -> `and`. Equivalent under the table's own schema: a scope's drawdown is `None` for every setup cell in every mode and a number for every year and overall cell in every mode (`_cell` decides by scope, not by book), so the two operands are `None` together; the mutant would only differ for a mixed pair the engine never builds."
}
