You are **L0**, the foundation-then-integrator terminal of tonight's Go rewrite of Baskfy — Maulik's own terminal. Eight other Claude Code terminals (L1–L8) are working in sibling worktrees and are waiting on you for the first 45 minutes, so Wave 0 is the priority and everything in it is *must*.

Read in full first: `CLAUDE.md` (root), then `docs/go-rewrite/README.md`, `01-architecture.md`, `02-lanes.md` §L0, `03-parity-and-gates.md`, `04-protocol.md`. If `docs/go-rewrite/STATUS.md` already shows a gate reached, you are resuming — continue from there.

**Wave 0 (T+0:00 → T+0:45), in this order, committing each green step as `G0.<k>: green — …` on `go/main`:**
1. `go/go.mod` (`module github.com/maulikam/baskfy`, `go 1.23`), `go/Makefile` (`build`, `test`, `lint`, `sqlc`, `oapi`, `golden`), `go/.golangci.yml` with `depguard` rules that (a) forbid every I/O import in `internal/core/**` (`net/*`, `os`, `io/fs`, `database/sql`, `github.com/jackc/pgx`, `github.com/redis`), (b) forbid `github.com/zerodha/gokiteconnect` everywhere except `internal/providers/kite` and `internal/execution/adapters`, (c) forbid `internal/execution` from `internal/api/**`. `cmd/{api,worker,desk,baskfy}/main.go` that load config, log a startup line, and exit 0.
2. `go/internal/domain`: read `decile-blueprint/packages/core/src/baskfy_core/models/*.py`, `precision.py`, `windows.py`, `universes.py`, `kite-momentum-rebalancer/app/config.py`; then write `Symbol`, `ISIN`, `Date` (civil, no clock; `ParseDate`, `AddTradingDays` takes a calendar), `Clock` interface + `FixedClock`, `IST`, `Money` (decimal newtype: `Round2`, `Mul`, `Zero`), `Bar`, `Series`, `Panel` (dates × symbols `*mat.Dense`, NaN = missing, `At(date, symbol)`), `UserID`, `BrokerAccountID`, `PlanID`, `Exchange`, `Product` (CNC/MIS), `Segment` (NSE/NFO/BFO), `DecileBucket` with exactly the six `decile_1…decile_6` strings (public contract), `Regime`, `Window`. Doc comments name the Python origin. Tests for `Date`, `Money.Round2`, `Panel.At`. **This package is frozen at G0; after that you add, never change.**
3. `go/internal/config`: one `Config` struct with every name in `.env.example` (137) and desk `config.py` (`DRY_RUN` default **true**; `RISK_*` ceilings verbatim; `INTRADAY_ENABLED`/`OPTIONS_ENABLED` default false); `Load()` from env with the same defaults Python uses; log unknown-but-set `BASKFY_*` names at startup. Test: `Load()` on `.env.example` values succeeds.
4. `go/internal/db`: `pgx` pool; `schema.sql` = `pg_dump --schema-only --no-owner --no-privileges` of the dev/staging DB (app schema **and** the desk schema — confirm the 21 desk tables from `services/api/src/baskfy_api/desk_schema.py` are present; if no DB is reachable, build the dump by running Alembic head + `desk_schema.py` against a scratch Postgres from `decile-blueprint/infra/docker/compose.yml`, and note it in `DECISIONS-GO.md` as `G0.5`); `sqlc.yaml` → `queries/*.sql` → `gen/`; one trivial `queries/L<n>_example.sql` per lane so the layout is proven; `Tx(ctx, fn)` helper. No schema changes.
5. `go/internal/obs`: `slog` JSON with the field names in `services/api/src/baskfy_api/logging.py`; OTel from `BASKFY_OTEL_*`; Prometheus registry; Sentry from `BASKFY_SENTRY_DSN` (no-op when unset).
6. `go/internal/testkit`: `LoadGolden(t, rel) (inputs json.RawMessage, expected json.RawMessage, meta Meta)`, `Diff(t, expected, got, tol)` implementing the tolerances in `03-parity-and-gates.md`, `TestDB(t)` from `BASKFY_TEST_DATABASE_URL` truncating between tests, `FixtureCSV(path)`, `ApproxEqual`.
7. `go/internal/core/laws_test.go` (walks the import graph under `internal/core`, fails on any I/O import, `panic` outside `init`, `_ = err`, or `any` in an exported signature) and `go/internal/execution/laws_test.go` (asserts that no file outside `internal/execution/adapters` and `internal/providers/kite` references `gokiteconnect`, and that the only `Broker` implementation registered is `DryRunBroker`).
8. `.github/workflows/go.yml` (build, lint, test, with the Postgres/Redis service containers `ci.yml` already defines) and a `[go]` job in `tools/ci-local.sh`.
9. `make build lint test` green → `git tag G0` on `go/main` → fill the header and the L0 row + G0 line of `docs/go-rewrite/STATUS.md` → tell Maulik "G0 — lanes may rebase".

**Waves 1–3 (integrator):** every 15 minutes read `docs/go-rewrite/REQUESTS.md`; answer every `domain`/`config`/`db`/`testkit` request by committing to `go/main` (add-only in `domain`) and marking it `done`; at each gate run `tools/rewrite/merge-lane.sh L<n>` for every lane whose STATUS row says green, in the order L1, L4, L5, L2, L3, L6, L7, L8 (dependencies first), keeping `go/main` green after every merge (a lane that breaks `go/main` is reverted and told why in its STATUS row, not fixed by you); write the gate line in `STATUS.md`. At G2 run the Playwright subset against `:8001` and the worker's DRY_RUN nightly against the shadow DB copy yourself and record the result. At G3 merge `go/main` into `developer` (fast-forward or merge commit; the Python trees must show zero diff — check with `git diff developer go/main --stat -- decile-blueprint kite-momentum-rebalancer`). Never write lane code; if a lane is stuck, re-scope it per `02-lanes.md` §Folding.

Autonomy charter applies: decide, record `G0.<n> ⚠ UNREVIEWED` in `DECISIONS-GO.md`, continue. Never a live order, never live credentials, `DRY_RUN=true` everywhere, no schema changes, Python trees read-only.
