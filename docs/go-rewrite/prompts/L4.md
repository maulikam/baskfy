You are one of nine Claude Code terminals rewriting Baskfy's Python backend in Go tonight. You are **L4**, working in this git worktree on branch `go/L4`. Other lanes are working in parallel in sibling worktrees; you never touch their directories or branches.

Read, in this order and in full, before writing any code:
1. `CLAUDE.md` at the repo root — the safety rails, the two laws, the desk's seven non-negotiables, the screener's nine house rules and the Autonomy charter all apply verbatim.
2. `docs/go-rewrite/README.md` — the decision, the timeline, the non-negotiables of this run (Python trees are read-only; DRY_RUN everywhere; no schema changes; generated contracts; goldens).
3. `docs/go-rewrite/01-architecture.md` — the Go layout, the locked stack, the two laws in Go form, what replaces DataFrames, conventions.
4. `docs/go-rewrite/02-lanes.md` — **your lane's section in full**; skim the others so you know who owns what.
5. `docs/go-rewrite/03-parity-and-gates.md` and `04-protocol.md`.
6. `docs/go-rewrite/STATUS.md` and `docs/go-rewrite/status/L4.md` — if they show progress, you are **resuming**: continue from the first 🟡/❌ item and do not redo ✅ work.

Hard rules for your terminal:
- **Never edit anything under `decile-blueprint/` or `kite-momentum-rebalancer/`.** They are the read-only source of truth. The only new files allowed near Python are golden dumpers in `tools/parity/dump_L4*.py`, which import Python and never modify it.
- **`DRY_RUN=true`; never live credentials; never an order path.** If you find yourself writing code that could reach Kite's order or GTT endpoints, stop: only `internal/execution/adapters/dryrun.go` exists tonight and the live adapter returns `ErrLiveOrdersDisabled`.
- **No schema changes, no migrations, no new env names.** The database and `.env.example` are contracts.
- Write only under the directories `02-lanes.md` gives your lane, plus `go/internal/db/queries/L4_*.sql`, `go/testdata/golden/L4/`, `tools/parity/dump_L4*.py`, `docs/go-rewrite/status/L4.md`, your row in `STATUS.md`, and your entries in `DECISIONS-GO.md`, `REQUESTS.md`, `NEEDS-MAULIK.md` (`## Go rewrite` heading). Need something else? Append to `REQUESTS.md` and code against a stub in your own package — never block.
- Generated code (`go/internal/api/server/**`, `go/internal/db/gen/**`) is regenerated (`make oapi`, `make sqlc`), never hand-edited.
- Commit every green step: `GL4.<k>: green — <one sentence>`; goldens as `GL4.golden: <what>`. Never commit red. Never commit `.env*`, `data/`, tokens.
- Update your row in `docs/go-rewrite/STATUS.md` and `status/L4.md` **every 30 minutes** — loud about what is not done.
- Autonomy charter: questions to Maulik are the exception. Decide, record `⚠ UNREVIEWED` in `DECISIONS-GO.md` as `GL4.<n>`, continue. Only Maulik's hands (credentials, logins, money) go to `NEEDS-MAULIK.md`; keep working on what does not need them.
- Rebase onto `go/main` at every gate (`git fetch . go/main && git rebase go/main`); resolve conflicts in generated dirs by regenerating.

Your first 45 minutes (Wave 0) — do these while L0 builds the foundation, they need no Go:
a. Read your Python scope in full. Write the inventory table in `docs/go-rewrite/status/L4.md`: one row per Python file → planned Go file, with the public functions each exposes and which existing tests/fixtures exercise them.
b. Write `tools/parity/dump_L4.py` using `tools/parity/golden.py`, run it with the tree's own venv, and commit goldens under `go/testdata/golden/L4/` for at least your *must* scope. No network, no live DB: if a function reads rows, dump the rows as inputs.
c. Draft the exported Go signatures for your *must* scope as a `doc.go` in each of your packages (types and function signatures with doc comments naming the Python origin) so consumers can code against them from G0.
Then, once `go/main` carries the `G0` tag (check `git log go/main --oneline | head`), rebase and port: must → should → could, one Python file at a time, golden test first, then the port, then `make lint`, then commit.

Definition of done for every `<stem>.go`: a `<stem>_test.go` whose cases are goldens or ported test intent; `make lint` clean; every exported identifier's doc comment names its Python origin; divergences recorded in `DECISIONS-GO.md`; `status/L4.md` marks it ✅.

Run until your *could* scope is done or until T+7:00, whichever first; then write your final status line, make sure everything green is committed on `go/L4`, and stop. Do not summarize between files — the status page is the summary.

## L4 specifics — providers + execution first, the desk console after

Scope `02-lanes.md` §L4. This is the safety boundary of the whole product; read `packages/execution/src/baskfy_execution/*.py`, `kite-momentum-rebalancer/app/kite_client.py`, `core/gateway.py`, `token_store.py` and the desk `CLAUDE.md` before anything.

**Wave 1 (must, G1):** `go/internal/providers/{ratelimit,circuit,retry,tokens,records,ports}.go`, `providers/kite` (data only: instruments, historical chunked by `BASKFY_KITE_MAX_DAYS_PER_REQUEST`, holdings = `quantity + t1_quantity + collateral_quantity`, positions, margins) behind a Redis token bucket **using exactly the key names Python uses** (read `ratelimit.py`; Python and Go share the bucket during the strangler window), `providers/nse` (same headers, cookie jar, same file discipline as `nse.py`), and `go/internal/execution/` — gateway order guards → risk → rate limit → journal → broker, `client_id = plan_id:symbol`, `user_id`+`broker_account_id` on every order with refusal on mismatch, SGB*/G-sec and `EXCLUDED_SYMBOLS` refused before any network call, product gates (CNC only; MIS needs `INTRADAY_ENABLED`, NFO/BFO needs `OPTIONS_ENABLED`, both default off), `StopFromVol` 8–12 %, and a GTT stop method **inside the gateway** (this closes the M16 caveat in CLAUDE.md — record it as `G4.1`). Write `execution/nonnegotiables_test.go` with seven named tests, one per non-negotiable. The only broker that compiles is `adapters/dryrun.go`; `adapters/kite_live.go` exists as a stub whose every method returns `ErrLiveOrdersDisabled`, and `laws_test.go` asserts that `gokiteconnect` order/GTT symbols appear nowhere else in the module. Token store: `fernet-go` must decrypt the existing encrypted token file format (`tokens.py`, `token_store.py`) — golden it with a throwaway key and a throwaway token, never a real one.

**Waves 2–3 (should/could):** `go/internal/desk` + `cmd/desk` on `:8421`, reading the desk's Postgres schema (the 21 `CREATE TABLE`s in `app/analytics/db.py` / `pg.py`; the schema dump in `go/internal/db/schema.sql` already has them). `/analyze` → plan with `plan_id` and 30-minute expiry; `/execute` requires `confirm=true` + a live `plan_id` and in DRY_RUN simulates end-to-end through the gateway and journals as Python does; then the JSON `/…/data` routes, then the HTML pages from the 12 Jinja templates via `html/template` (static copied verbatim), then `cmd/baskfy desk daily|autorun|friday-drill|backup` subcommands. The G3 golden is `scripts/friday_drill.py`'s plan for a fixed holdings snapshot + scan CSV: the Go plan's lines must equal it. The Python desk stays the only thing that trades until two shadow Fridays — never wire, flag or document otherwise.
