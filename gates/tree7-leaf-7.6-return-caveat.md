# Gates: Tree 7 leaf 7.6 — price-return caveat component

Scope: Price-return ~1.2%/yr caveat on every money/return surface, not only backtest.

ABANDON: deferred this pass — `ReturnConventionNote` already on basket detail; catalog cards got a line in audit; full sweep not done.

- [ ] G1: Shared component (or ReturnConventionNote) on catalog cards + investment returns
  EVIDENCE: pending — deferred

- [ ] G2: Disclaimer-style automated sweep or vitest coverage for curated return surfaces
  EVIDENCE: pending — deferred
