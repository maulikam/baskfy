# Pinned to the versions this project is verified against. Unpinned ranges let
# Starlette 1.6 land mid-session and broke the webview with a 500; a trading
# system should not change behaviour because a transitive release shipped.
# Refresh deliberately:  uv pip install -U <pkg>  then re-pin.
fastapi==0.141.1
uvicorn[standard]==0.52.3
jinja2==3.1.6
# Imported directly by the strangle config loader. It was arriving only as a transitive
# dependency of uvicorn[standard], so the day that changed, config/strangle*.yaml would
# have stopped loading with an ImportError on a scheduled run.
pyyaml==6.0.3
python-multipart==0.0.32
pandas==3.0.5
numpy==2.5.2
kiteconnect==5.2.1
python-dotenv==1.2.2
orjson==3.11.9
uvloop==0.22.1; sys_platform != 'win32'
pytest==9.1.1
httpx2==2.10.0            # starlette TestClient
# future engines (uncomment when enabling):
# py_vollib>=1.0.1   # option greeks

# --- M20 §1: observability, all three optional ------------------------------------------------
# app/telemetry.py guards every import and degrades to no-ops, so none of these is required for
# the desk to run or to trade. They are pinned here so `pip install -r` gives a machine that CAN
# be observed; the box installs what it wants. Each view is still off unless its environment
# variable is set.
prometheus_client==0.24.1
