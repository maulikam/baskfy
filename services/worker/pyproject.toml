[project]
name = "decile-worker"
version = "0.1.0"
description = "Decile Celery workers, Beat schedule and the nightly pipeline."
requires-python = ">=3.12,<3.13"
dependencies = [
    "decile-core",
    "decile-api",
    "decile-providers",
    # docs/02 §"The decision in one table": Celery + Celery Beat, `flower` for inspection.
    "celery[redis]>=5.4",
    "flower>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/decile_worker"]

[tool.uv.sources]
decile-core = { workspace = true }
decile-api = { workspace = true }
decile-providers = { workspace = true }
